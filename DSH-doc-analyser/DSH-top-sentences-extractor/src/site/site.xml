<project name="${project.name}">
	<version position="right" />
	<publishDate position="right" format="yyyy-MM-dd - hh:mm:ss" />
	<bannerLeft>
		<name>DSH-top-sentences-extractor</name>
		<src>${logo.image}</src>
		<href>${main.server}/site/${release.type}/products/DSH-top-sentences-extractor</href>
	</bannerLeft>
	<skin>
		<groupId>${skin.groupId}</groupId>
		<artifactId>${skin.artifactId}</artifactId>
		<version>${skin.version}</version>
	</skin>
	<body>
<!--		<links>
			<item name="Maven" href="http://maven.apache.org" />
		</links> -->
		<menu ref="parent" />
		<menu ref="modules" />
		<menu ref="reports" />
	</body>
</project>